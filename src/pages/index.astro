---
import Base from "../layouts/Base.astro";
import SeasonHome from "../components/SeasonHome.astro";
import { currentSeason, seasons } from "../lib/data";

if (!seasons.length) throw new Error("No seasons found. Run: ./ffnews ingest && ./ffnews facts");
---
<Base title="Pressbox — The Privacy Project" season={currentSeason}>
  <SeasonHome year={currentSeason} />
</Base>
